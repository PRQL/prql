[package]
description = "PRQL is a modern language for transforming data — a simpler and more powerful SQL."
edition = "2021"
license = "Apache-2.0"
name = "prql"
version = "0.0.1"

[dependencies]
pest = "^2.1"
pest_derive = "^2.1"

serde = "^1.0"
# [dev-dependencies]
insta = "^1.11"
