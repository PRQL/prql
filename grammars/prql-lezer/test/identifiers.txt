# Basic identifier

filter foo

==>

Query(Statements(PipelineStatement(Pipeline(CallExpression(Ident,Ident)))))

# Identifier with underscore and digit

filter foo_123

==>

Query(Statements(PipelineStatement(Pipeline(CallExpression(Ident,Ident)))))

# Unicode identifier

filter räksmörgås

==>

Query(Statements(PipelineStatement(Pipeline(CallExpression(Ident,Ident)))))
