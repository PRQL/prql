# This workflow runs on release tags. It also runs on nightly builds without
# publishing anything, in order to test as much of the build works as possible.
#
# We indicate whether it should publish, vs. just build, by checking
# `github.event_name == 'push' && github.ref_type == 'tag'`. Both halves are
# needed. `nightly.yaml` calls this workflow, and inside a called workflow the
# `github` context is the caller's — a push to `main` — so the tag test is what
# separates a release from a smoke test. And `workflow_dispatch` is excluded
# deliberately: it can be aimed at an existing tag by anyone with write access,
# which would let a non-maintainer fire the publish jobs.
#
# GitHub's immutable-releases setting refuses asset uploads to a release that is
# already published, so the release is created as a draft, every artifact is
# uploaded to the draft, and `publish-release` publishes it once they have all
# landed. That is also why the trigger is the tag rather than the release:
# creating a draft emits no `release` event, so there would be nothing to start
# this workflow while the release is still writable.

name: release
on:
  push:
    tags:
      - "[0-9]+.[0-9]+.[0-9]+*"
  workflow_call:
  workflow_dispatch:

# We need consistent env vars across all workflows for the cache to work
env:
  CARGO_TERM_COLOR: always
  CLICOLOR_FORCE: 1
  RUSTFLAGS: "-C debuginfo=0"
  RUSTDOCFLAGS: "-Dwarnings"

jobs:
  # The release is created here, as a draft, so that the build jobs have
  # somewhere to upload to. Doing it in one place rather than letting each
  # upload create the release on demand keeps several concurrent jobs from
  # racing to create it.
  create-draft:
    runs-on: ubuntu-24.04
    permissions:
      contents: write
    steps:
      - name: 📂 Checkout code
        uses: actions/checkout@v7
      - name: Create the draft release
        if: ${{ github.event_name == 'push' && github.ref_type == 'tag' }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG: ${{ github.ref_name }}
        run: |
          # An existing draft is reused, so the notes can be written by hand
          # before the tag is pushed. An existing *published* release is fatal:
          # its assets are already locked, so every upload below would fail
          # with a 422 partway through the run.
          if state=$(gh release view "$TAG" --json isDraft --jq '.isDraft' 2>/dev/null); then
            if [ "$state" = "true" ]; then
              echo "Draft release $TAG already exists; uploading into it."
              exit 0
            fi
            # Which recovery applies turns on the release's immutability.
            # Deleting a mutable release leaves its tag in place, so the fix is
            # a re-run rather than another tag push — re-pushing the tag would
            # change nothing and start nothing. Deleting an immutable release
            # is allowed too, but GitHub does not let its tag name be reused
            # afterwards, so the re-run would fail at `gh release create` and
            # the version has to be re-cut:
            # https://docs.github.com/en/code-security/concepts/supply-chain-security/immutable-releases
            #
            # This is a second call rather than another field on the one above
            # because a `gh` too old for `isImmutable` would fail that call and
            # skip the whole guard; a failure here costs only the specific
            # remedy, on a path that is aborting anyway. It gets its own
            # message rather than joining the mutable case: deleting an
            # immutable release cannot be undone, so "unknown" must not read as
            # "safe to delete".
            if immutable=$(gh release view "$TAG" --json isImmutable --jq '.isImmutable' 2>/dev/null); then
              if [ "$immutable" = "true" ]; then
                echo "::error::Release $TAG is already published and immutable, so its assets cannot be changed and its tag cannot be reused after a delete. Release the next patch version instead."
              else
                echo "::error::Release $TAG is already published, so its assets cannot be changed. Delete the release, then re-run all jobs on this run."
              fi
            else
              echo "::error::Release $TAG is already published, so its assets cannot be changed, and its immutability could not be read. Check it before deleting: a mutable release can be deleted and all jobs on this run re-run, but an immutable one leaves its tag name unusable, so the next patch version has to be released instead."
            fi
            exit 1
          fi
          # The changelog entry is the release's notes, so a tag pushed before
          # the changelog is updated fails here rather than publishing a
          # release with nothing in it. This job checks out the tag, so the
          # section has to be in the tagged commit: adding it to `main` and
          # re-running reads the same file, which is why the message asks for
          # the tag to move. Moving a tag is an `update` against the all-tags
          # rulesets and so needs admin, hence the second route — the check
          # above reuses a draft, so one made by hand carries the notes in.
          awk -v tag="$TAG" '
            $0 ~ "^## " tag "([^0-9.]|$)" { found = 1; next }
            found && /^## / { exit }
            found { print }
          ' CHANGELOG.md > notes.md
          if [ ! -s notes.md ]; then
            echo "::error::No CHANGELOG.md section for $TAG. Add it on main, then move the tag to that commit and re-push it — or, without admin to move a tag, create the draft release by hand and re-run this run."
            exit 1
          fi
          gh release create "$TAG" --draft --title "$TAG" --notes-file notes.md

  brew-dispatcher:
    name: Release on homebrew-prql
    runs-on: ubuntu-24.04
    environment: release
    needs: create-draft
    if: ${{ github.event_name == 'push' && github.ref_type == 'tag' }}
    permissions:
      contents: read
    steps:
      - uses: actions/github-script@v9
        with:
          github-token: ${{ secrets.TEND_BOT_TOKEN }}
          script: |
            await github.rest.actions.createWorkflowDispatch({
              owner: 'prql',
              repo: 'homebrew-prql',
              workflow_id: 'update.yaml',
              ref: 'main',
              inputs: {
              version: '${{ github.ref }}',
              URL: 'https://github.com/PRQL/prql/archive/${{ github.ref }}.tar.gz'
              }
            })

  build-prqlc:
    needs: create-draft
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-24.04
            target: x86_64-unknown-linux-musl
          - os: ubuntu-24.04
            target: aarch64-unknown-linux-musl
          - os: macos-15
            target: aarch64-apple-darwin
          - os: windows-latest
            target: x86_64-pc-windows-msvc
          # Intel macOS build
          - os: macos-15-intel
            target: x86_64-apple-darwin
            features: default,test-dbs
    permissions:
      contents: write
    steps:
      - name: 📂 Checkout code
        uses: actions/checkout@v7
      - uses: ./.github/actions/build-prqlc
        id: build-artifact
        with:
          target: ${{ matrix.target }}
          profile: release
          features: cli
      - name: Upload release artifact
        if: ${{ github.event_name == 'push' && github.ref_type == 'tag' }}
        uses: softprops/action-gh-release@efb35369e0ad2afab669f228072c1b0d510eae64 # v3.0.3
        with:
          tag_name: ${{ github.ref_name }}
          draft: true
          append_body: true
          files: ${{ steps.build-artifact.outputs.artifact-name }}
      - name: test the CLI works
        # TODO: Add for Windows too (but will require unzipping rather than
        # un-taring)
        #
        # Currently filtering by x86, since that's the same as those not
        # cross-compiled. But we'll have to refine this in the future.
        if:
          ${{ contains(matrix.target, 'x86') && matrix.target !=
          'x86_64-pc-windows-msvc' }}
        run: |
          # `prqlc` is an existing path at the root
          mkdir -p temp_path
          tar xzf ${{ steps.build-artifact.outputs.artifact-name }} -C temp_path
          ./temp_path/prqlc --help

  build-prqlc-c:
    # Mostly a copy/paste of `build-prqlc`.
    needs: create-draft
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-24.04
            target: x86_64-unknown-linux-musl
          - os: ubuntu-24.04
            target: aarch64-unknown-linux-musl
          - os: macos-15
            target: aarch64-apple-darwin
          - os: windows-latest
            target: x86_64-pc-windows-msvc
    permissions:
      contents: write
    steps:
      - name: 📂 Checkout code
        uses: actions/checkout@v7
      - uses: ./.github/actions/build-prqlc-c
        id: build-artifact
        with:
          target: ${{ matrix.target }}
          profile: release
      - name: Upload release artifact
        if: ${{ github.event_name == 'push' && github.ref_type == 'tag' }}
        uses: softprops/action-gh-release@efb35369e0ad2afab669f228072c1b0d510eae64 # v3.0.3
        with:
          tag_name: ${{ github.ref_name }}
          draft: true
          append_body: true
          files: ${{ steps.build-artifact.outputs.artifact-name }}

  publish-winget:
    runs-on: ubuntu-24.04
    environment: release
    needs: publish-release
    if: ${{ github.event_name == 'push' && github.ref_type == 'tag' }}
    permissions:
      contents: read
    steps:
      - name: publish
        uses: vedantmgoyal2009/winget-releaser@4ffc7888bffd451b357355dc214d43bb9f23917e # v2
        with:
          identifier: PRQL.prqlc
          version: ${{ github.ref_name }}
          installers-regex: '^prqlc-.*-windows-.*\.zip$'
          token: ${{ secrets.TEND_BOT_TOKEN }}
          fork-user: prql-bot

  build-deb-package:
    runs-on: ubuntu-24.04
    strategy:
      fail-fast: false
      matrix:
        target:
          - x86_64-unknown-linux-musl
          - aarch64-unknown-linux-musl
    needs: build-prqlc
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v8
        with:
          name: prqlc-${{ matrix.target }}-release
      - name: Copy files into .deb package
        run: |
          tar -xf prqlc-*.tar.gz
          mkdir -p .debpkg/usr/bin
          mv prqlc .debpkg/usr/bin/prqlc
          chmod +x .debpkg/usr/bin/prqlc
      - name: Set arch variable
        run: |
          if [ "${{ matrix.target }}" == "aarch64-unknown-linux-musl" ]; then
            echo "package_arch=arm64" >> "$GITHUB_ENV"
          elif [ "${{ matrix.target }}" == "x86_64-unknown-linux-musl" ]; then
            echo "package_arch=amd64" >> "$GITHUB_ENV"
          else
            echo "::error::Unknown architecture: ${{ matrix.target }}"
            exit 1
          fi
      - name: 📦 Build .deb package
        uses: jiro4989/build-deb-action@a883c65147d80579cb359548b9a902ff0a35ae5b # v4.3.0
        with:
          package: prqlc
          package_root: .debpkg
          maintainer: The PRQL Project
          version:
            ${{ (github.event_name == 'push' && github.ref_type == 'tag') &&
            github.ref_name || 0 }}
          arch: ${{ env.package_arch }}
          desc: >
            prqlc is the CLI for the PRQL compiler. It compiles PRQL to SQL, and
            offers various diagnostics.

            PRQL is a modern language for transforming data — a simple,
            powerful, pipelined SQL replacement.
      - uses: actions/upload-artifact@v7
        with:
          name: deb-${{ matrix.target }}
          path: ./*.deb
      - name: Release
        if: ${{ github.event_name == 'push' && github.ref_type == 'tag' }}
        uses: softprops/action-gh-release@efb35369e0ad2afab669f228072c1b0d510eae64 # v3.0.3
        with:
          tag_name: ${{ github.ref_name }}
          draft: true
          files: prqlc_*.deb

  build-rpm-package:
    runs-on: ubuntu-24.04
    strategy:
      fail-fast: false
      matrix:
        target:
          - x86_64-unknown-linux-musl
          #- aarch64-unknown-linux-musl # https://github.com/jiro4989/build-rpm-action/issues/6
    needs: build-prqlc
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v8
        with:
          name: prqlc-${{ matrix.target }}-release
      - name: Copy files into .rpm package
        run: |
          tar -xf prqlc-*.tar.gz
          mkdir -p .rpmpkg/usr/bin
          mv prqlc .rpmpkg/usr/bin/prqlc
          chmod +x .rpmpkg/usr/bin/prqlc
      - name: Set variables
        run: |
          if [ "${{ matrix.target }}" == "aarch64-unknown-linux-musl" ]; then
            echo "package_arch=aarch64" >> "$GITHUB_ENV"
          elif [ "${{ matrix.target }}" == "x86_64-unknown-linux-musl" ]; then
            echo "package_arch=x86_64" >> "$GITHUB_ENV"
          else
            echo "::error::Unknown architecture: ${{ matrix.target }}"
            exit 1
          fi
      - name: 📦 Build .rpm package
        uses: jiro4989/build-rpm-action@f11474937f502aaa8bb36d8c1a8ec6f8de536a0c # v2.5.0
        with:
          summary: CLI for PRQL, a modern language for transforming data
          package: prqlc
          package_root: .rpmpkg
          maintainer: The PRQL Project
          vendor: The PRQL Project
          version:
            ${{ (github.event_name == 'push' && github.ref_type == 'tag') &&
            github.ref_name || 0 }}
          arch: ${{ env.package_arch }}
          desc: >
            prqlc is the CLI for the PRQL compiler. It compiles PRQL to SQL, and
            offers various diagnostics.

            PRQL is a modern language for transforming data — a simple,
            powerful, pipelined SQL replacement.
          license: Apache-2.0
      - uses: actions/upload-artifact@v7
        with:
          name: artifact-rpm
          path: |
            ./*.rpm
            !./*-debuginfo-*.rpm
      - run: rm prqlc-debuginfo-*.rpm
      - name: Release
        if: ${{ github.event_name == 'push' && github.ref_type == 'tag' }}
        uses: softprops/action-gh-release@efb35369e0ad2afab669f228072c1b0d510eae64 # v3.0.3
        with:
          tag_name: ${{ github.ref_name }}
          draft: true
          files: prqlc-*.rpm

  publish-release:
    runs-on: ubuntu-24.04
    needs:
      - create-draft
      - build-prqlc
      - build-prqlc-c
      - build-deb-package
      - build-rpm-package
    if: ${{ github.event_name == 'push' && github.ref_type == 'tag' }}
    permissions:
      contents: write
    steps:
      - name: Publish the release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_REPO: ${{ github.repository }}
          TAG: ${{ github.ref_name }}
        # `gh release edit` sends `tag_name` with the request; a hand-rolled
        # PATCH that omits it resets a draft's tag to `untagged-<hex>`.
        run: gh release edit "$TAG" --draft=false

  build-and-publish-snap:
    runs-on: ubuntu-24.04
    environment: release
    needs: create-draft
    if: ${{ github.event_name == 'push' && github.ref_type == 'tag' }}
    permissions:
      contents: read
    steps:
      - name: 📂 Checkout code
        uses: actions/checkout@v7
      - name: Move Snap to project root directory
        run: cp -r prqlc/packages/snap/ .
      - name: 📦 Build Snap
        id: build
        uses: snapcore/action-build@3bdaa03e1ba6bf59a65f84a751d943d549a54e79 # v1.3.0
      - name: 🆙 Publish Snap
        if: ${{ github.event_name == 'push' && github.ref_type == 'tag' }}
        uses: snapcore/action-publish@214b86e5ca036ead1668c79afb81e550e6c54d40 # v1.2.0
        env:
          SNAPCRAFT_STORE_CREDENTIALS:
            ${{ secrets.SNAPCRAFT_STORE_CREDENTIALS }}
          # Credentials are Candid-format (`snapcraft export-login`); snapcraft
          # requires this to use them.
          SNAPCRAFT_STORE_AUTH: candid
        with:
          snap: ${{ steps.build.outputs.snap }}
          release: stable

  build-python-wheels:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: true
      matrix:
        os: [ubuntu-24.04, windows-latest]
        package:
          - prqlc-python
        target: [x86_64]
        include:
          # MacOS with universal builds
          - os: macos-15
            package: prqlc-python
            target: universal2-apple-darwin
          # Also produce more targets for ubuntu:
          - os: ubuntu-24.04
            package: prqlc-python
            target: aarch64
          - os: ubuntu-24.04
            package: prqlc-python
            target: source
    permissions:
      contents: read
    steps:
      - name: 📂 Checkout code
        uses: actions/checkout@v7
      - uses: ./.github/actions/build-python
        with:
          target: ${{ matrix.target }}
          package: ${{ matrix.package }}
          profile: release

  publish-python:
    runs-on: ubuntu-24.04
    needs: [build-python-wheels, create-draft]
    if: ${{ github.event_name == 'push' && github.ref_type == 'tag' }}
    environment: release
    permissions:
      id-token: write
    strategy:
      matrix:
        package:
          - prqlc-python
    steps:
      - uses: actions/download-artifact@v8
        with:
          # `*` covers target & OS
          pattern: ${{ matrix.package }}-*-release
          merge-multiple: true
      - name: Publish to PyPI
        uses: PyO3/maturin-action@e83996d129638aa358a18fbd1dfb82f0b0fb5d3b # v1.51.0
        with:
          command: upload
          args: --skip-existing *

  # `release.yaml` is reused from `nightly.yaml` on PRs, where it only
  # smoke-tests the publish path. Those runs are on branch refs, which the
  # `release` environment's deployment policy denies, so each smoke test is its
  # own job holding no `id-token: write`. A registry accepts an OIDC token from
  # any run of this workflow unless its publisher config pins the environment,
  # so a run outside `release` must not be able to mint one.
  test-publish-js:
    runs-on: ubuntu-24.04
    if: ${{ !(github.event_name == 'push' && github.ref_type == 'tag') }}
    permissions:
      contents: read
    steps:
      - name: 📂 Checkout code
        uses: actions/checkout@v7
      - uses: ./.github/actions/build-js
      # `--provenance` needs an OIDC token, so it belongs to `publish-js` alone.
      - name: Check the npm package
        run: npm publish --dry-run --access public
        working-directory: prqlc/bindings/js/

  publish-js:
    runs-on: ubuntu-24.04
    needs: create-draft
    if: ${{ github.event_name == 'push' && github.ref_type == 'tag' }}
    environment: release
    permissions:
      contents: read
      id-token: write
    steps:
      - name: 📂 Checkout code
        uses: actions/checkout@v7
      - uses: ./.github/actions/build-js
      - name: Publish to npm
        run: npm publish --provenance --access public
        working-directory: prqlc/bindings/js/

  test-publish-to-cargo:
    runs-on: ubuntu-24.04
    if: ${{ !(github.event_name == 'push' && github.ref_type == 'tag') }}
    permissions:
      contents: read
    steps:
      - name: 📂 Checkout code
        uses: actions/checkout@v7
      - uses: baptiste0928/cargo-install@8195d4f734a149db85385bb4102b42efcd373759 # v3.5.0
        with:
          crate: cargo-release
      # Currently, we can only check prqlc-parser which is not dependent other local crates with --dry-run.
      # https://github.com/crate-ci/cargo-release/issues/691
      # --no-verify is required to prevent build.
      - run:
          cargo release publish --no-confirm --no-verify --package prqlc-parser

  publish-to-cargo:
    runs-on: ubuntu-24.04
    needs: create-draft
    if: ${{ github.event_name == 'push' && github.ref_type == 'tag' }}
    environment: release
    permissions:
      contents: read
      id-token: write
    steps:
      - name: 📂 Checkout code
        uses: actions/checkout@v7
      - uses: baptiste0928/cargo-install@8195d4f734a149db85385bb4102b42efcd373759 # v3.5.0
        with:
          crate: cargo-release
      # Auth is via crates.io Trusted Publishing (GitHub Actions OIDC). One
      # exchange covers prqlc, prqlc-parser and prqlc-macros: crates.io scopes
      # the minted token to every crate whose config matches this repo,
      # workflow and environment.
      - name: Authenticate with crates.io
        id: auth
        uses: rust-lang/crates-io-auth-action@c6f97d42243bad5fab37ca0427f495c86d5b1a18 # v1.0.5
      - run: cargo release publish --no-confirm --execute
        env:
          CARGO_REGISTRY_TOKEN: ${{ steps.auth.outputs.token }}

  # Requires another pass: https://github.com/PRQL/prql/issues/850
  # publish-prql-java:
  #   runs-on: ubuntu-24.04
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v7
  #     - name: Install Java and Maven
  #       uses: actions/setup-java@v3
  #       with:
  #         java-version: 8
  #     - name: Release Maven package
  #       uses: samuelmeuli/action-maven-publish@v1
  #       with:
  #         gpg_private_key: ${{ secrets.gpg_private_key }}
  #         gpg_passphrase: ${{ secrets.gpg_passphrase }}
  #         nexus_username: ${{ secrets.nexus_username }}
  #         nexus_password: ${{ secrets.nexus_password }}
  #         directory: prql-java/java/

  publish-web:
    needs: create-draft
    if: ${{ github.event_name == 'push' && github.ref_type == 'tag' }}
    uses: ./.github/workflows/publish-web.yaml
    permissions:
      contents: read
      pages: write
      id-token: write

  # `web` tracks the latest release, so doc-only fixes backport onto it. A push
  # with the default token starts no workflow, so `publish-web` is the release's
  # only publish.
  push-web-branch:
    runs-on: ubuntu-24.04
    needs: create-draft
    if: ${{ github.event_name == 'push' && github.ref_type == 'tag' }}
    permissions:
      contents: write
    steps:
      - name: 📂 Checkout code
        uses: actions/checkout@v7
      - run: git push origin HEAD:web --force

  push-devcontainer:
    needs: create-draft
    if: ${{ github.event_name == 'push' && github.ref_type == 'tag' }}
    uses: ./.github/workflows/build-devcontainer.yaml
    secrets: inherit
    with:
      push: true
