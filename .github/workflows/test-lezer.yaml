name: test-lezer

on:
  pull_request:
    paths:
      - "grammars/prql-lezer/**"
      - ".github/workflows/test-lezer.yaml"
  workflow_call:

concurrency:
  # See notes in `pull-request.yaml`
  group: ${{ github.workflow }}-${{ github.ref }}-lezer
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 📂 Checkout code
        uses: actions/checkout@v3
      - name: 🧅 Setup Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest
      - name: Install dependencies
        run: bun install
      - name: Build grammar
        run: bun run build
      - name: Test grammar
        run: bun test
