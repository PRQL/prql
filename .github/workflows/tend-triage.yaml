# Generated by tend. Regenerate with: uvx tend init

name: tend-triage
on:
  issues:
    types: [opened]

concurrency:
  group: ${{ github.workflow }}-${{ github.event.issue.number }}
  cancel-in-progress: true

jobs:
  triage:
    # Skip prql-bot-authored issues (to avoid loops from tend itself),
    # except nightly-failure issues which carry the `github_actions` label —
    # we want those triaged. NOTE: this file is normally generated by
    # `uvx tend init`; this condition is a local customization.
    if: >-
      github.event.issue.user.login != 'prql-bot' ||
      contains(github.event.issue.labels.*.name, 'github_actions')
    runs-on: ubuntu-24.04
    timeout-minutes: 60
    permissions:
      contents: write
      pull-requests: write
      id-token: write
      actions: read
      issues: write
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
          fetch-tags: true
          token: ${{ secrets.PRQL_BOT_GITHUB_TOKEN }}

      - uses: ./.github/actions/tend-setup

      - uses: max-sixty/tend@v1
        with:
          github_token: ${{ secrets.PRQL_BOT_GITHUB_TOKEN }}
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          bot_name: prql-bot
          prompt: |
            /tend-ci-runner:triage ${{ github.event.issue.number }}
