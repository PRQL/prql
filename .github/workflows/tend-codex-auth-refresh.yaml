# Generated by tend 0.3.1. Regenerate with: uvx tend@latest init
#
# Do not edit this file directly — it will be overwritten on regeneration.
# To change this workflow, edit .config/tend.yaml. To change what the agent
# does, edit the relevant skill (for example, `running-tend`) in this repo's
# .claude/skills/ directory, or open an issue at
# https://github.com/max-sixty/tend/issues for changes that need to
# happen upstream in the tend-ci-runner plugin.

name: tend-codex-auth-refresh
on:
  schedule:
    - cron: "17 3 * * 0"
  workflow_dispatch:

# Only this workflow may rotate the refresh-token chain. Consumer jobs share
# the access token concurrently; a second refresher must never race this one.
concurrency:
  group: tend-codex-auth-refresh
  cancel-in-progress: false

jobs:
  refresh:
    if: github.repository_owner == 'PRQL'
    environment:
      name: tend
      deployment: false
    runs-on: ubuntu-24.04
    permissions:
      contents: read
    steps:
      - uses: max-sixty/tend/codex/refresh@0.3.1
        with:
          codex_auth_json: ${{ secrets.CODEX_AUTH_JSON }}
          codex_refresh_auth_json: ${{ secrets.CODEX_REFRESH_AUTH_JSON }}
          refresh_pat: ${{ secrets.CODEX_REFRESH_PAT }}
