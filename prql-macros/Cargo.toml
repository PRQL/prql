[package]
edition = "2021"
name = "prql-macros"
publish = false
version = "0.2.5"

[lib]
proc_macro = true

[dependencies]
prql-compiler = {path = "../prql-compiler"}
syn = "1.0"

[package.metadata.release]
shared-version = true
tag-name = "{{version}}"
tag-prefix = ""
