# Compute the 3 longest songs for each genre and sort by genre
# mssql:test
from.tracks
select {genre_id,milliseconds}
group {genre_id} (
  sort {-milliseconds}
  take 3
)
join from.genres (==genre_id)
select {name, milliseconds}
sort {+name,-milliseconds}
