# sqlite:skip
# mssql:skip
# mysql:skip
# clickhouse:skip
# glaredb:skip (the string_agg function is not supported)
from tracks
filter genre_id == 100
derive empty_name = name == ''
aggregate {
  s = (sum track_id | as REAL),
  c = concat_array name,
  a = all empty_name,
  n = any empty_name,
}
